<%= render 'navidation' %>

<h1>Sites edit</h1>

<%= render 'errors' %>

<div>
<%= form_for(@site) do |f| %>

  <%= f.label :id %>
  <%= f.text_field :id %>


  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :feed_url %>
  <%= f.text_field :feed_url %>

  <%= f.submit "Update site", class: "btn btn-primary" %>

<% end %>
</div>
