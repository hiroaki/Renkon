<h1>Editing item</h1>

<%= render "form", item: @item %>

<br>

<div>
  <%= link_to "Show this item", channel_item_path(@channel, @item) %> |
  <%= link_to "Back to items", channel_items_path(@channel) %>
</div>
