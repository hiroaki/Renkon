<h2>channel.items</h2>

<ul id="items" class="list-disc">
  <% @items.each do |item| %>
    <li>
      <%= item.title %>
    </li>
  <% end %>
</ul>

<div class="contaner">
  <%= link_to "Back to channels", channels_path %>
</div>
